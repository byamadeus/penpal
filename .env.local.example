# Email Secret Token
# This token must be included in the email subject line: [TOKEN-your-token-here] Post Title
EMAIL_SECRET_TOKEN=your-secret-token-here

# This file is for local development only
# In production, these are set as:
# - GitHub Secrets (EMAIL_SECRET_TOKEN)
# - Cloudflare Worker Environment Variables (EMAIL_SECRET_TOKEN, GITHUB_TOKEN, GITHUB_REPO)
