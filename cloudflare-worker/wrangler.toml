name = "penpal-email-worker"
main = "email-worker.js"
compatibility_date = "2024-01-01"

# Email routing configuration
# Run: wrangler deploy to deploy this worker

[vars]
# Public variables (non-sensitive)
# GITHUB_REPO will be set as environment variable in Cloudflare dashboard

# Secrets (set via: wrangler secret put <NAME>)
# - EMAIL_SECRET_TOKEN
# - GITHUB_TOKEN
# - GITHUB_REPO
