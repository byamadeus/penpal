# Email-to-Blog Environment Variables
# Copy this to .env.local for local development

# Secret token for email authentication
# This must match the token you put in your email subjects: [TOKEN-xxx]
EMAIL_SECRET_TOKEN=your-secret-token-here

# For local testing only - not needed in production
# The actual secrets are stored in GitHub Secrets and Cloudflare Worker
